[project]
name = "dev-marketer"
version = "0.1.0"
description = "Real‑time launch analytics & growth recommendations (demo)"
authors = [{name = "Axentx AI"}]
requires-python = ">=3.9"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
